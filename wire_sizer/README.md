# Customizable Wire Sizer
Before running the wire sizer, you must first perform a bit of setup.

Install python 3.12+

```shell
python -m venv .venv && pip install -r requirements
python wire_sizer.py 200 1
```